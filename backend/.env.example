GROQ_API_KEY=gsk_tu_key_aqui
PORT=3001

# Origen del frontend, para CORS con cookies (credentials: true).
# Debe coincidir exactamente con donde corre el frontend.
FRONTEND_URL=http://localhost:5173

# Opcional: ruta del archivo SQLite. Por defecto backend/data/gestor.db
# (se crea solo la primera vez que arranca el backend).
# DB_PATH=./data/gestor.db
